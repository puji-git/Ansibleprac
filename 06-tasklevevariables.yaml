- name: playlevel variables
  hosts: local
  connection: local
  vars:
    course: "devops"
    duration: 10Hr
    trainer: shiva
  tasks:
  - name: print course information
    vars:
      course: ansible
    ansible.builtin.debug:
      msg: "devops {{ course }}, {{ duration }}, {{ trainer }} "